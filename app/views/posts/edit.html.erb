<h1>Edit Post</h1>

<%= form_with(model: @post, local: true) do |form| %>
  <div>
    <%= form.text_field :body %>
  </div>

  <div>
    <%= form.submit "Post" %>
  </div>
<% end %>

<br \>
<div>
  <%= link_to "Cancel", root_path %>
</div>
